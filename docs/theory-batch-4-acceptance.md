# Theory batch 4 acceptance boundary

This batch is accepted for review only if all of the following remain true:

- the four pages retain separate responsibilities for Kohn–Sham construction, XC approximation, SCF solution, and finite representation;
- every mathematical expression is native MathML with exactly one TeX annotation;
- no client JavaScript, packaged font, new production dependency, CMS, search index, or general content registry is introduced;
- all thirteen reviewed Theory pages remain available through the Theory directory;
- the built output contains exactly nineteen HTML documents and no JavaScript or font assets;
- the obsolete Cambridge Linear Algebra identifier remains absent;
- program completion, SCF convergence, representation convergence, observable convergence, and scientific support remain distinct claims;
- external software examples do not become transferable parameter recommendations.

A passing build establishes only the covered source, route, serialization, and static-runtime constraints. It does not independently validate every scientific statement or learning outcome.
