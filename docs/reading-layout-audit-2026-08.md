# Full-width reading and source-semantics audit

- observed_at: 2026-08-01 18:59 CST (Asia/Shanghai)
- baseline: `6b1dee8e0517e03c99542fc01697ddf3fd9785e6`
- scope: the built home and entry routes, Part I–VII indexes, all 46 Martin
  units, all 10 Sholl–Steckel units, the SCF lab, shared components, navigation,
  TOC, responsive CSS, and the validation registries

## Findings

| Area | Baseline observation | Required correction |
| --- | --- | --- |
| Unit frame | Three MDX routes explicitly use `ReadingColumn`; 22 outline routes use `ReadingOutline`; other substantive units rely on compatibility CSS. | Resolve every registered unit through one shared route-aware frame rather than duplicating wrapper imports. |
| Wide reading | `--sl-content-width` is 68–72rem, but explicit reading columns remain 47rem and Starlight divides the remaining width around a fixed sidebar/TOC. | Add Parallel, Focus, and Atlas canvas modes with bounded column widths and an ultra-wide three-track layout. |
| Left navigation | Autogenerated Part groups expose large nested trees. | Keep Part I–VII visible, collapse inactive Parts, and route the complete inventory to a flat Theory Atlas. |
| Right TOC | Built pages expose ordinary heading depth without source-layer meaning. Substantive units commonly contain more H2 headings than catalog sections. | Render catalog sections separately from authored exposition, derivation, literature, and open-question entries. |
| Source semantics | `SourceNote`, component eyebrows, and ad hoc headings carry useful evidence, but there is no all-unit registry or stable DOM contract. | Add catalog-derived source headers, layer markers, a 46-unit structural audit, and a review queue. |
| Strong headings | The built Martin pages contain scoped source titles as well as several authored headings using `完整`, `精确`, `真实`, `full`, or `complete`. Mechanical replacement would damage valid catalog wording. | Preserve source titles; correct only high-confidence authored drift and queue body-level cases. |
| Literature | No separate literature registry, claim ledger, discussion contract, or routes exist. | Add empty static-first contracts and lightweight Chapter bridges without summaries or inferred claims. |
| Mobile/no-JS | Existing bilingual fallbacks and formula-local overflow are present. | Preserve them while adding reading-width persistence and 2560px acceptance. |

## Built baseline

- pages: 80;
- dist: 35,073,983 bytes;
- files: 422;
- home JavaScript: 6,168 bytes;
- representative theory JavaScript: 13,643 bytes;
- largest asset: 1,433,139 bytes;
- registered deterministic validators: 37;
- registered browser smokes: 38.

## Migration boundary

The implementation will use Starlight component overrides, route/catalog data,
shared reading components, and build-output validation. Existing Chapter,
Appendix, practice, and lab prose remains unchanged except for high-confidence
heading, label, locator, or source-layer corrections. Body-level semantic drift
is recorded for later review rather than rewritten in this task.
