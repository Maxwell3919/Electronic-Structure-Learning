# Theory batch 6 merge boundary

The branch preserves the current main fix that treats absolute links to other GitHub Pages projects as external. The expanded browser smoke applies the base-path escape guard only to authored local references. This is an implementation compatibility note, not a new scientific requirement.
