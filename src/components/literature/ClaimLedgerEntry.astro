---
const { claim } = Astro.props;
---
<article class="esl-claim-ledger-entry" data-claim-id={claim.claimId} data-assessment={claim.currentAssessment}>
  <h2>{claim.claimText}</h2>
  <p>Assessment: {claim.currentAssessment}</p>
  <p>Uncertainties: {claim.uncertainties}</p>
</article>
