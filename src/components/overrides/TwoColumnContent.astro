<div class="esl-three-track">
  <aside class="right-sidebar-container print:hidden"><div class="right-sidebar"><slot name="right-sidebar" /></div></aside>
  <div class="main-pane"><slot /></div>
</div>

<style>
  .main-pane { min-width: 0; isolation: isolate; }
  @media (min-width: 72rem) {
    .esl-three-track { display: grid; grid-template-columns: minmax(0, 1fr) minmax(12rem, 15rem); align-items: start; }
    .right-sidebar-container { grid-column: 2; grid-row: 1; min-width: 0; }
    .right-sidebar { position: sticky; top: var(--sl-nav-height); max-height: calc(100vh - var(--sl-nav-height)); overflow-y: auto; border-inline-start: 1px solid var(--esl-border); }
    .main-pane { grid-column: 1; grid-row: 1; }
  }
</style>
