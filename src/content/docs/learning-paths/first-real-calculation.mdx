---
title: "第一次真实计算"
description: "真实计算前的对象、离散、SCF、目标量和证据门。"
sidebar:
  order: 2
---

import SiteSectionHero from '../../../components/site/SiteSectionHero.astro';
import LearningPathTimeline from '../../../components/site/LearningPathTimeline.astro';
import learningPaths from '../../../data/site/learningPaths.mjs';

export const path = learningPaths.find((item) => item.id === 'first-real-calculation');

<SiteSectionHero titleZh={path.titleZh} titleEn={path.titleEn} description={path.summaryZh} descriptionEn={path.summaryEn} pageType="learning" />

<LearningPathTimeline path={path} />

本页不提供 QE/VASP 输入、参数或伪造结果；实验将在后续独立任务完成。
