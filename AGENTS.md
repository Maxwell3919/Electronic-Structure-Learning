# AGENTS.md

本文件规定人类维护者、网页版 GPT、Talos Codex 和其他自动化 Agent 在本仓库中的工作边界。

## 1. 项目定位

本仓库是电子结构与密度泛函理论学习网站的活动源码权威。它保存：

- Astro/Starlight 网站代码；
- 原创学习正文与章节导航；
- 小型、可检查的可视化数据；
- 可视化组件、数据契约和验证脚本；
- 构建与部署配置。

它不保存教材 PDF、批量论文、原始 DFT 输出、WFC/DWF、restart、CHGCAR、WAVECAR、POTCAR、许可证材料、凭据或大型数据包。

## 2. 工作开始前

1. 读取远端 `main` 的当前 SHA、`README.md`、本文件和与任务相关的 `docs/`。
2. 检查现有 Issue、PR 和任务分支，避免重复或冲突工作。
3. 使用单一职责的短期分支；网页版 GPT 的写入必须通过 PR。
4. 不根据聊天记录或旧工作树假定远端状态。

## 3. 内容规则

每个正式学习单元至少需要说明：

- 学习问题与前置知识；
- 来源及可定位范围；
- 概念、定义和物理对象；
- 公式中的定义、假设、近似和推导连接；
- 可视化模型、输入、输出和验收标准；
- 数值方法、收敛对象和适用边界；
- 练习或复盘问题。

不得把来源主张、教学模型或单次运行写成独立验证事实。程序正常退出、SCF 收敛、结构收敛、目标 observable 收敛和科学结论成立必须分别判断。

## 4. 版权与来源

- 不提交教材 PDF、扫描页、受版权保护图表或大段转录。
- 章节标题、书目信息和页码定位可用于导航；正文必须原创表述。
- 自制图与交互模型必须说明其模型边界，不得伪装成教材原图或真实材料结果。
- 外部代码、数据和图片进入仓库前必须检查许可、来源和可再分发性。

详见 `docs/source-and-copyright-policy.md`。

## 5. 代码与数据

- 默认使用 TypeScript 严格模式。
- 交互组件必须具备无 JavaScript 时仍可理解的文本说明。
- 小型数据优先使用 JSON、CSV、CIF 或 XYZ，并附 provenance/metadata。
- 真实 DFT 计算在外部计算环境完成；网站只接收经过解析、降采样和验证的派生数据。
- 不为了视觉效果隐藏单位、参考能、采样路径、展宽、归一化或计算条件。

## 6. 验证与合并

最低合并门槛：

1. 依赖锁文件与实际安装一致；
2. `npm run build` 在声明的 Node 版本下通过；
3. 内部链接、MDX 导入和公式渲染无构建错误；
4. 新增可视化具有输入、输出、边界和至少一个可重复检查；
5. PR 差异只包含当前任务范围；
6. 不含敏感内容和不应进入 Git 的大型文件。

只有实际执行并满足上述标准后，才能描述为构建通过。普通文件存在、代码审阅或 GitHub API 回读不等于运行验证。

## 7. 部署边界

当前目标是静态网站。Cloudflare Pages 或 GitHub Pages 的公网发布、域名切换、仓库可见性变化和新增服务端后端属于单独变更，不由普通内容 PR 自动授权。
