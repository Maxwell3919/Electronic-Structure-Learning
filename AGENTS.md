# AGENTS.md

## 项目目标

维护一个可连续阅读、可交互、可核验的电子结构学习网站。内容以 Martin 第二版的章节结构作为导航主线，但所有解释、推导、示意图、练习和交互实现必须重新创作。

## 开始工作前

1. 读取最新 `main`、本文件、`project/architecture.md` 和 `project/content-contract.md`。
2. 检查目标章节的教材章节号、标题和起始页是否与教材目录及现有索引一致。
3. 只修改当前任务涉及的章节、组件、数据和测试；不要顺便重构全站。
4. 网页版 GPT 使用短期 branch/PR；Talos 在写入前先 fetch 并确认工作树状态。

## 内容证据边界

- `sourcePage` 只表示教材章节起始页，不证明网站解释正确。
- 教材直接记录、作者解释、网站编写者推断和独立数值验证必须分开表达。
- 不把单次运行、图形相似或格式检查写成科学验证。
- 不复制原书正文、习题、长表格或原图；短标题和章节结构仅用于导航与引用。
- 真实 DFT 输入输出留在项目仓库或计算主机；网站只保存适合 Git 的小型派生数据与 provenance。

## 章节最小结构

每个实质章节应逐步补齐：

1. 阅读定位与先修条件；
2. 核心问题；
3. 必要公式及符号定义；
4. 推导中的关键假设；
5. 可视化或最小教学模型；
6. 与真实 DFT 工作流的连接；
7. 适用边界、常见误判与验证方式；
8. 小结和练习。

骨架页可以暂时保留 `未开始`，但不得用模板化空话伪装成完成内容。

## 代码与数据

- 默认使用 TypeScript、Astro 组件和浏览器原生 API。
- 只有交互复杂度确实需要时才引入 React、Plotly、Three.js、3Dmol.js 或 Pyodide。
- 新增依赖必须说明用途、影响范围和静态站点兼容性。
- 数据文件必须包含来源、生成方法、单位、字段含义和适用边界。
- 禁止提交 PDF 教材、批量 raw output、WFC/DWF/restart、凭据或许可证受限内容。

## 验证门

提交前至少运行：

```bash
npm run validate
npm run check
npm run build
```

交互组件还应检查窄屏、键盘操作、标签、单位、初始状态和无 JavaScript 时的可读说明。
