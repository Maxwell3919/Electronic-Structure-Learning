# AGENTS.md

本文件规定人类维护者、网页版 GPT、Talos Codex 和其他自动化 Agent 在本仓库中的工作边界。

## 1. 项目定位

本仓库是公开的电子结构与密度泛函理论学习网站源码权威，保存：

- Astro/Starlight 网站代码；
- Martin 7 Parts / 46 单元的目录骨架；
- Sholl–Steckel 10 章实践交叉参考；
- 原创学习正文与章节导航；
- 小型、可检查的公开可视化数据；
- 组件、数据契约和验证脚本；
- GitHub Pages 构建与部署配置。

它不保存教材 PDF、批量论文、原始 DFT 输出、WFC/DWF、restart、CHGCAR、WAVECAR、POTCAR、许可证材料、凭据、私密记录或大型数据包。

## 2. 工作开始前

1. 读取远端 `main` 当前 SHA、`README.md`、本文件和相关 `docs/`；
2. 检查现有 Issue、PR 和任务分支；
3. 使用单一职责短期分支；网页版 GPT 写入必须通过 PR；
4. 不根据聊天记录或旧工作树假定远端状态；
5. 仓库为 public，提交前按公开发布标准检查全部新增内容。

## 3. 完整框架规则

结构权威：

```text
src/data/martin/index.mjs
src/data/shollSteckelStructure.mjs
```

必须保持：

- Martin：7 Parts、28 Chapters、18 Appendices、46 单元；
- Sholl–Steckel：10 个实践参考章节；
- 单元 slug 唯一；
- 页面文件与目录条目一一对应；
- 分节页码不早于所属单元起始页。

运行：

```bash
npm run validate:framework
```

不得在 56 个单元页面中各自复制维护一份分节目录；页面应从 catalog 读取。

## 4. 内容规则

正式内容至少说明：

- 学习问题与前置知识；
- 来源及可定位范围；
- 概念、物理对象、符号、单位与边界条件；
- 公式中的定义、严格关系、假设、近似和数值离散；
- 可视化模型、输入、输出、验收标准和不能支持的结论；
- 数值方法、目标 observable 和收敛边界；
- 练习或复盘问题。

程序正常退出、SCF 收敛、结构收敛、目标 observable 收敛和科学结论支持必须分别判断。

## 5. 版权与来源

- 不提交教材 PDF、扫描页、受版权保护图表、习题原文或大段转录；
- 章节标题、书目信息和页码可用于导航；
- 正文必须原创表述；
- 自制图和教学模型必须说明边界；
- 外部代码、数据和图片进入仓库前必须检查许可、来源和可再分发性。

## 6. 代码与数据

- 默认使用 TypeScript 严格模式；
- 交互组件必须有无 JavaScript 时仍可理解的文本说明；
- 小型数据优先使用 JSON、CSV、CIF 或 XYZ，并附 provenance；
- 真实 DFT 计算在外部环境完成；
- 不隐藏单位、参考能、采样路径、展宽、归一化或计算条件。

## 7. 验证与合并

最低门槛：

1. 锁文件与实际安装一致；
2. `npm run check` 在 Node 22.12 环境通过；
3. 内部链接、MDX 导入和公式渲染无构建错误；
4. 新增可视化有输入、输出、边界和至少一个重复检查；
5. PR 只包含当前任务范围；
6. 不含敏感内容、受限制材料或大型文件。

## 8. 部署边界

仓库和 GitHub Pages 站点均已公开：

```text
https://maxwell3919.github.io/Electronic-Structure-Learning/
```

`main` 更新后可自动部署。自定义域名、后端、账户、数据库以及任何可能暴露私密或受版权保护材料的发布仍需单独批准。
