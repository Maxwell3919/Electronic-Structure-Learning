{
  "name": "electronic-structure-atlas",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A minimal, static atlas for electronic-structure theory, methods, tools, and references.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate:clean-slate": "node scripts/validate-clean-slate.mjs --source",
    "validate:build-budget": "node scripts/validate-build-budget.mjs",
    "check": "astro check && npm run validate:clean-slate && astro build && npm run validate:build-budget && node scripts/validate-clean-slate.mjs --built"
  },
  "dependencies": {
    "astro": "7.1.4"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
